apiVersion: v1
kind: Service

metadata:
  name: auth-service
  namespace: togglemaster

spec:
  selector:
    app: auth-service

  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http

  type: ClusterIP # Acessível apenas dentro do cluster